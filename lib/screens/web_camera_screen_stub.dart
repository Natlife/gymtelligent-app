import 'package:flutter/material.dart';

/// Mobile stub — never instantiated; exists only so the conditional
/// import in web_camera_screen.dart compiles on non-web targets.
class WebCameraScreenImpl extends StatelessWidget {
  const WebCameraScreenImpl({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
